<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= groupName %> Exercises</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      max-width: 1200px;
      margin: auto;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
    }

    .exercise-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }

    .exercise-card {
      display: flex;
      flex-direction: column;
      width: 220px;
      padding: 10px;
      background: #fefefe;
      border: 1px solid #ddd;
      border-radius: 8px;
      text-align: center;
      text-decoration: none;
      color: #333;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }

    .exercise-card:hover {
      transform: scale(1.03);
    }

    .exercise-card img {
      width: 100%;
      height: 150px;
      object-fit: cover;
      margin: 10px 0;
      border-radius: 6px;
    }

    .exercise-title {
      font-weight: bold;
      font-size: 1.1em;
      margin-bottom: 5px;
    }

    .exercise-description {
      font-size: 0.9em;
      color: #666;
    }

    .no-image {
      width: 100%;
      height: 150px;
      background-color: #eee;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #aaa;
      margin: 10px 0;
      border-radius: 6px;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 40px;
      color: #007bff;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1><%= groupName %> Exercises</h1>

  <div class="exercise-grid">
    <% exercises.forEach(ex => { %>
      <a class="exercise-card" href="/exercises/<%= ex.id %>">
        <div class="exercise-title"><%= ex.name %></div>

        <% if (ex.thumbnail) { %>
          <img src="<%= ex.thumbnail %>" alt="<%= ex.name %>">
        <% } else { %>
          <div class="no-image">No Image</div>
        <% } %>

        <div class="exercise-description"><%= ex.description || 'No description' %></div>
      </a>
    <% }) %>
  </div>

  <div style="text-align: center; margin-top: 30px;">
  <a href="/exercises" style="
    display: inline-block;
    padding: 12px 24px;
    background-color: #28a745;
    color: white;
    text-decoration: none;
    font-weight: bold;
    border-radius: 6px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    transition: background-color 0.2s ease;
  " onmouseover="this.style.backgroundColor='#218838'" onmouseout="this.style.backgroundColor='#28a745'">
    ⬅️ Back to Exercise
  </a>
</div>
</body>
</html>
